import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.flowpack.predictiveflow',
  appName: 'predictiveflow',
  webDir: 'dist'
};

export default config;
